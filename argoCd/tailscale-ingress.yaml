apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tailscale-argo-cd-ingress
  namespace: argocd
spec:
  defaultBackend:
    service:
      name: argocd-server
      port:
        number: 443
  ingressClassName: tailscale
